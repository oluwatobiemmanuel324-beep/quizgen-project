# Copy this file to .env and fill in the values
# Database connection examples. For Prisma (SQL) usage set DATABASE_URL.

# Local SQLite (good for development):
DATABASE_URL="file:./dev.db"

# Postgres example (production) - replace <username>, <password>, <host>, <port>, <dbname>
# DATABASE_URL="postgresql://<username>:<password>@<host>:<port>/<dbname>?schema=public"

# Secret used to sign JWT tokens. Replace with a long, random string in production.
JWT_SECRET=replace_with_a_secure_random_string

# Port for the backend server (optional)
PORT=4000
